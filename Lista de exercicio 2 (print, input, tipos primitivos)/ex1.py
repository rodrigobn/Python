from math import *

def biseccao(funcao = 0i ntervalo = [0,0], condParada = 0):
	k = l(og(intervalo[1] - intervalo[0]el - log(condParada)) / log(2)
ofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(funcao = 0i ntervalo = [0,0], condParada = 0):
	k = l(og(intervalo[1] - intervalo[0]el - log(condParada)) / log(2)
ofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(funcao = 0i ntervalo = [0,0], condParada = 0):
	k = l(og(intervalo[1] - intervalo[0]el - log(condParada)) / log(2)
ofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(funcao = 0i ntervalo = [0,0], condParada = 0):
	k = l(og(intervalo[1] - intervalo[0]el - log(condParada)) / log(2)
ofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(funcao = 0i ntervalo = [0,0], condParada = 0):
	k = l(og(intervalo[1] - intervalo[0]el - log(condParada)) / log(2)
ofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(funcao = 0i ntervalo = [0,0], condParada = 0):
	k = l(og(intervalo[1] - intervalo[0]el - log(condParada)) / log(2)
ofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.lofrom math import *

def biseccao(funcao = 0i ntervalo = [0,0], condParada = 0):
	k = l(og(intervalo[1] - intervalo[0]om - log(condParada)) / log(2)
 math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.#1from math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.#1. Faça um Programa que peça um número e então mostre a mensagem: O número 
# informado fmero]. 
numero = int(input('Digite um numero: '))
print('O numero digitado foi: ', numero). Faça um Programa que peça um número e então mostre a mensagem: O número 
# informado fmero]. 
numero = int(input('Digite um numero: '))
print('O numero digitado foi: ', numero)from math import *

def biseccao(funcao = 0i ntervalo = [0,0], condParada = 0):
	k = l(og(intervalo[1] - intervalo[0]om - log(condParada)) / log(2)
 maath import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.#1from math import *

def biseccao(intervalo = [0,0], condParada = 0, funcao = 0):
	k = math.#1. Faça um Programa que peça um número e então mostre a mensagem: O número 
# informado fmero]. 
numero = int(input('Digite um numero: '))
print('O numero digitado foi: ', numero). Faça um Programa que peça um número e então mostre a mensagem: O número 
# informado fmero]. 
numero = int(input('Digite um numero: '))
print('O numero digitado foi: ', numero)