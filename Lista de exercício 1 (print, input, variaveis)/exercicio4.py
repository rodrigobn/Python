#d	Faça um algoritmo para ler a base e a altura de um triângulo. 
#emseguida, escreva a área do mesmo. Obs.: Área = (Base * Altura) / 2

base = float(input("Base: "))
altura = float(input("Altura: "))

area = (base * altura) / 2

print (area)